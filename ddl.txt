CREATE TABLE table_m3 (
    "Row ID" INT,
    "Order ID" VARCHAR(50),
    "Order Date" VARCHAR(20),
    "Ship Date" VARCHAR(20),
    "Ship Mode" VARCHAR(50),
    "Customer ID" VARCHAR(50),
    "Customer Name" VARCHAR(100),
    "Segment" VARCHAR(50),
    "Country" VARCHAR(50),
    "City" VARCHAR(100),
    "State" VARCHAR(100),
    "Postal Code" VARCHAR(20),
    "Region" VARCHAR(50),
    "Product ID" VARCHAR(50),
    "Category" VARCHAR(50),
    "Sub-Category" VARCHAR(50),
    "Product Name" VARCHAR(255),
    "Sales" FLOAT,
    "Quantity" INT,
    "Discount" FLOAT,
    "Profit" FLOAT
);

COPY table_m3 ("Row ID", "Order ID", "Order Date", "Ship Date", "Ship Mode", "Customer ID", 
	"Customer Name", "Segment", "Country", "City", "State", "Postal Code", "Region", "Product ID", 
	"Category", "Sub-Category", "Product Name", "Sales", "Quantity", "Discount", "Profit")
FROM '/var/lib/postgresql/data/P2M3_Stefano_Veronigo_Wijaya_data_raw.csv'   -- Raw Data
WITH (FORMAT csv, HEADER true, ENCODING 'LATIN1');